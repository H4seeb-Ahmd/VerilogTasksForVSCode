{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile And Run Verilog",
            "type": "shell",
            "command": "",
            "args": [
                "iverilog -o ${fileDirname}/${fileBasenameNoExtension}.vvp ${file} && vvp ${fileDirname}/${fileBasenameNoExtension}.vvp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
